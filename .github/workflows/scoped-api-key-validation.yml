name: Scoped API Key Validation

on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  scopes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - run: pytest tests/test_api_key_scopes.py tests/test_scope_defaults.py
